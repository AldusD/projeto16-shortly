const TABLES = Object.freeze({
    USERS: 'users',
    SESSIONS: 'sessions',
    SHORTS: 'shorts',
    VISITS: 'visits'
});
const { USERS, SESSIONS, SHORTS, VISITS } = TABLES;
export { USERS, SESSIONS, SHORTS, VISITS };